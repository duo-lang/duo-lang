module Subsumption.CE_056f;

import Codata.Function;
import Data.Bool;
import Data.Peano;

-- Subsumptions which shouldn't hold

def rec prd ex21    : (rec b. < Z , S( b ) > ) -> (rec c. < Z , S( c ) > )                  := ex21;
def rec prd ex21sub : forall t0. (t0 -> (rec r4.(t0 \/ < S( r4 ) >)))                       := ex21;
