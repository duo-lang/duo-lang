-- We can only use a definition with the annotated type, not the inferred type.

destructor Ap : CBN;
constructor True : CBV;
constructor False : CBV;
constructor Bar : CBV;

def id[*] : { Ap(<True , False >)[<True , False>] } := cocase { Ap(x)[*] => x };

def useId[*] : < Bar > := id.Ap(Bar)[*];
