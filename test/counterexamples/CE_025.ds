-- We can only use a definition with the annotated type, not the inferred type.

destructor Ap : CBN;
constructor Bar : CBV;
def id[*] : { Ap(<True , False >)[<True , False>] } := cocase { Ap(x)[k] => x >> k };

def useId[*] : < Bar > := mu k.(id >> Ap(Bar)[k]);
