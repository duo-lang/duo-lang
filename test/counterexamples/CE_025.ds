-- We can only use a definition with the annotated type, not the inferred type.

destructor Ap : CBN;
constructor Bar : CBV;
prd id : { Ap(<True , False >)[<True , False>] } := cocase { Ap(x)[k] => x >> k };

prd useId : < Bar > := mu k.(id >> Ap(Bar)[k]);
