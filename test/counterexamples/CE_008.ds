# Should not typecheck since Bool is incompatible with Bool2

data Bool { True, False };
data Bool2 { TT, FF };

def fail := match True with { TT => 'None
                            , FF => 'None
                            };

