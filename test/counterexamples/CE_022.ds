# The annotated type is more general than the inferred one.
destructor Ap : CBN;
constructor True : CBV;
constructor False : CBV;
prd foo : forall a. { Ap(a)[a] } := comatch { Ap(x)[k] =>
                       x >> match { True => True >> k,
                                    False => False >> k
                                    }};
