# Should not typecheck since types of match cases do not coincide

data Bool { True, False };
data Bool2 { TT, FF };

def fail := comatch { 'Ap(x) => match x with { TT => 'None
                                             , False => 'None
                                             }};
