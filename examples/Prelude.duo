-- Standard Prelude

def cmd impossibleCmd := ExitSuccess;
def prd impossiblePrd : Bot := mu k. impossibleCmd;
def cns impossibleCns : Top := mu x. impossibleCmd;

-- | Generic print consumer
def cns print : Top := mu x.Print(x); ExitSuccess;
