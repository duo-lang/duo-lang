# Standard Prelude

data Bool : Type CBV { True, False };
data Nat : Type CBV { Z, S(Nat) };
data NatList : Type CBV { Nil, Cons(Nat, NatList) };
data Unit : Type CBV { Unit };

# Generic print consumer
cns print := mu x.Print(x);