# Standard Prelude

import Function;
import Bool;
import Peano;
import LinearLogic;


cmd impossible := Done;
prd impossible := mu k. impossible;
cns impossible := mu x. impossible;

# Generic print consumer
cns print := mu x.Print(x); Done;
