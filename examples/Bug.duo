module Bug;

-- | The function type
codata Fun : (-a : CBV, +b : CBV) -> CBV {
    Ap(a,return b)
};

type operator -> rightassoc at 0 := Fun;

-- | Function composition
def prd compose :=
  \f g x => f (g x);
