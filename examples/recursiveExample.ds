import Prelude;

destructor Ap2(CBV,CBV)[CBV] : CBV;

prd rec addS := cocase { Ap2(x,y)[k] =>
                       x
                       >>
                       case { Z    => y >> k
                             , S(z) => addS >> Ap2(z,S(y))[k]
                             }
                   };

prd rec addA := \x => \y => case x of { Z => y
                                      , S(z) => addA z S(y)
					        };
		     


cmd main := Read[ mu x. Read[mu y. Print(addA x y);Done]];
